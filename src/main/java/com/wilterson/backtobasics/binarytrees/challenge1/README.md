# List Binary Tree Elements By Level

Creates a linked list of all the nodes at each depth of a binary tree, e.g., if you have a tree with depth D, you'll have D linked lists. The linked list is built at pre-order traversal time using recursion, thus its time complexity is O(log N) and space complexity is O(log N) as well.